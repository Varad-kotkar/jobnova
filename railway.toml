[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'cd /app/backend && alembic -c alembic.ini upgrade head && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
